
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: helmapplications.helmops.shijunlee.net
spec:
  group: helmops.shijunlee.net
  names:
    kind: HelmApplication
    listKind: HelmApplicationList
    plural: helmapplications
    singular: helmapplication
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HelmApplication is the Schema for the helmapplications API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HelmApplicationSpec defines the desired state of HelmApplication
            properties:
              steps:
                description: the install steps install
                items:
                  description: ComponentStep the step which will install
                  properties:
                    ValuesRefComponentRelease:
                      description: the values ref from before component release
                      items:
                        type: string
                      type: array
                    checkIfExistNotInstall:
                      description: notice this will check helm template resource ,
                        if have resource install , this will not install and only
                        install or update the old object, only support clusterrole
                        serviceaccount and other global resource
                      type: boolean
                    componentName:
                      description: the component will install
                      type: string
                    componentReleaseName:
                      description: the component release name ,if not set will use
                        the appname-chartname
                      type: string
                    parameters:
                      description: the component install params
                      items:
                        description: 'TODO: need add procees ComponentParameter the
                          component install param ,notice not both set value and ref
                          ,if set will use value as default'
                        properties:
                          name:
                            description: the parameter name
                            type: string
                          type:
                            description: the value type for parameter , will try to
                              convert value to the set type not set will use default
                              string
                            type: string
                          value:
                            description: the value for parameter ,notice not both
                              set value and ref ,if set will use value as default
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
            required:
            - steps
            type: object
          status:
            description: HelmApplicationStatus defines the observed state of HelmApplication
            properties:
              conditions:
                description: the install conditions
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
              currentStepName:
                description: current install step name use ComponentReleaseName for
                  default
                type: string
              status:
                description: the application install status
                type: string
              stepReturns:
                description: the return data for install steps
                items:
                  description: StepReturn the application install step return values
                    which define in componse
                  properties:
                    stepName:
                      type: string
                    values:
                      items:
                        description: CreateParam tpaas middleware app create param
                        type: object
                      type: array
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
